# 本地运行与构建排障教程

本教程目标：

1. 本地稳定启动前后端；
2. 在部署前完成构建验证；
3. 避免 `TS2688` 和 `fast-glob`/PostCSS 这类依赖损坏问题。

## 1. 环境要求

- Node.js：`>=20`（推荐 `20.20.x`）
- pnpm：`10.x`（前端统一使用 pnpm）
- npm：`10.x`（后端 `server/` 目录使用 npm）

检查版本：

```bash
node -v
pnpm -v
npm -v
```

## 2. 前端安装（根目录）

只使用 pnpm，不要在根目录混用 npm。

```bash
cd /Users/manlokyue/Documents/毕业设计/备份/网页设计
pnpm run setup:clean
pnpm run doctor:deps
```

如果 `doctor:deps` 失败，说明 `node_modules` 不完整，请重跑：

```bash
rm -rf node_modules
pnpm install --frozen-lockfile
pnpm run doctor:deps
```

## 3. 后端安装（server 目录）

```bash
cd /Users/manlokyue/Documents/毕业设计/备份/网页设计/server
npm install
```

## 4. 环境变量

### 4.1 前端（根目录 `.env`）

至少确认：

```env
VITE_API_URL=http://localhost:3001/api
```

### 4.2 后端（`server/.env`）

从示例复制并修改：

```bash
cd server
cp .env.example .env
```

核心项建议：

```env
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
```

## 5. 本地启动

终端 1（后端）：

```bash
cd /Users/manlokyue/Documents/毕业设计/备份/网页设计/server
npm run dev
```

终端 2（前端）：

```bash
cd /Users/manlokyue/Documents/毕业设计/备份/网页设计
pnpm dev
```

访问：

- 前端：`http://localhost:5173`
- 后端：`http://localhost:3001`

## 6. 功能验证（建议部署前必做）

1. 学生登录（如 `student001`）提交反馈；
2. 教师登录（如 `T001`）进入 `/feedback`；
3. 点击“导出当前可见数据 CSV”，确认可下载并打开；
4. 刷新页面，确认反馈列表和图表正常显示。

## 7. 构建验证（部署前）

```bash
cd /Users/manlokyue/Documents/毕业设计/备份/网页设计
pnpm run build:check
```

成功标准：

- `doctor:deps` 通过；
- `tsc -b` 成功；
- `vite build` 成功输出 `dist/`。

## 8. 常见错误与处理

### 8.1 `TS2688: Cannot find type definition file ...`

原因：依赖安装不完整或根目录混用了 npm/pnpm。

处理：

```bash
rm -rf node_modules
pnpm install --frozen-lockfile
pnpm run build
```

### 8.2 `Cannot find module './filters/deep'`（fast-glob / PostCSS）

原因：`fast-glob` 包损坏（常见于安装中断）。

处理：

```bash
rm -rf node_modules
pnpm install --frozen-lockfile
pnpm run doctor:deps
pnpm run build
```

### 8.3 `ENOTFOUND registry.npmjs.org`

原因：DNS 或网络不可达。

处理建议：

1. 先确认服务器能访问外网；
2. 配置可用 npm registry（公司源或镜像）；
3. 网络恢复后重跑安装命令。

## 9. 远程部署建议（最小流程）

```bash
# 1) 拉取代码
git pull

# 2) 前端
pnpm install --frozen-lockfile
pnpm run build:check

# 3) 后端
cd server
npm install
npm start
```

上线前保留一次 `build:check` 日志，可作为“可部署证据”。
