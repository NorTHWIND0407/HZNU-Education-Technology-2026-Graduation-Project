# 变更记录（CHANGELOG）

本文档用于统一维护项目的历史更改日期、版本与关键改动。

## 2026-03-16（v2.3 - 账号权限与微纪录片互动增强版）

- 账号体系重构为“账号+密码一致校验（不存密码）”：
  - 学生：`s+年级(3-9)+班级(01-10)+学号(01-45)`，示例 `s30101`。
  - 教师：`t+年级(3-9)+班级(01-10)`，示例 `t301`。
  - 管理员：`admin`。
- 登录规则改为严格格式校验，失败返回明确提示；首次登录自动建档并按账号推导角色、年级、班级。
- 学生/教师首次登录后的默认显示名统一为账号名（如 `s30101`、`t301`），满足课堂统一账号展示需求。
- 学生新增“修改姓名”入口：在导航栏右上角用户菜单可直接修改显示名为真实姓名。
- 后端用户更新权限收口：学生仅允许修改自己的姓名/头像，不允许修改班级与年级。

- 反馈权限增强：
  - 教师与管理员可按班级查看反馈数据。
  - 教师与管理员可修改、删除反馈（教师仅限本班）。
  - 学生可修改并撤回自己的反馈（撤回即删除本人记录）。
- 反馈接口补充/加固：
  - 新增通用修改接口 `PATCH /api/feedback/:id`。
  - 保留状态更新 `PATCH /api/feedback/:id/status` 并补充班级权限校验。
  - 删除接口 `DELETE /api/feedback/:id` 支持学生撤回本人反馈与教师/管理员按权限删除。

- 微纪录片页面新增互动区能力：
  - 视频下方新增“评论区”（默认折叠）。
  - “评论区”右侧新增点赞按钮，直接显示点赞数。
  - 点赞逻辑支持单用户点赞/取消点赞（类似 YouTube）。
  - 评论发布后即时追加到列表。
- 新增微纪录片实时同步（WebSocket）：
  - 点赞数实时广播更新。
  - 新评论实时广播更新。
- 新增微纪录片后端接口：
  - `GET /api/microdoc/:clipId`（读取评论+点赞态）
  - `POST /api/microdoc/:clipId/like`（点赞/取消点赞）
  - `POST /api/microdoc/:clipId/comments`（发布评论）
- 新增数据库持久化表：
  - `microdoc_comments`
  - `microdoc_likes`

- 评论权限开关新增：
  - 环境变量 `MICRODOC_COMMENT_LOGIN_REQUIRED`（默认 `true`）。
  - 开启时：游客可浏览评论但不能发布评论；登录用户可发布评论。
  - 已同步到 `server/.env.example`。

- 示例账号与文档同步更新为新规范：`s30101`、`t301`、`admin`。
- 构建与语法验证：多轮 `node --check` 与 `npm run build` 均通过。

## 2026-03-12（v2.2 - 反馈系统持久化数据库版）

- 将后端反馈系统数据库实现从内存 Mock 改为 SQLite 文件持久化（`sql.js`）。
- 服务启动时自动初始化数据库，默认数据文件为 `server/data/feedback.db`。
- `auth`、`feedback`、`stats`、`users` 路由统一改为使用 `server/db/index.js`。
- `server/scripts/init-db.js` 改为 SQLite 初始化脚本，支持重复执行。
- 新增数据库文件忽略规则：`.gitignore` 中忽略 `server/data/*.db`、`server/data/*.sqlite`。
- 验证结果：
  - 前端单元测试通过（`pnpm test`）。
  - 数据库链路自测通过（用户登录会话、反馈写入、统计读取）。

## 2026-03-10（文档整理）

- 新增统一变更记录：`CHANGELOG.md`。
- 将以下历史说明文档整合到本文件：
  - `AI模块配置完成说明.md`
  - `AI问答模块改进总结.md`
  - `滚动问题修复说明.md`
- 保留使用型文档：`README.md`、`本地运行教程.md`、`DEPLOY_MANUAL.md`、`Prompt编辑指南.md`。

## 2025-10-15（v2.1 - 滚动体验修复版）

- 修复 AI 流式输出期间“滚轮被强制拉回底部”的问题。
- 将自动滚动逻辑调整为“用户在底部时跟随，离开底部后暂停跟随”。
- 主要影响文件：`src/components/QAChat.tsx`。

## 2025-10-15（v2.0 - 智能交互增强版）

- 快捷问题支持一键提问，并扩展问题数量。
- 优化 AI 回答风格与 Prompt 结构，增强多样性和可读性。
- 新增 Prompt 使用与修改说明文档。
- 主要影响文件：
  - `src/components/QAChat.tsx`
  - `src/lib/aiClient.ts`
  - `Prompt编辑指南.md`

## 2025-10（v1.0 - AI 问答配置完成）

- 完成 AI 问答模块的真实接口接入（支持 Mock/真实模式切换）。
- 完成火山引擎客户端与统一 AI 客户端接入。
- 完成 AI 问答页面与聊天组件的基础能力（流式响应、多轮对话、快捷问题）。
- 主要影响文件：
  - `src/lib/volcengineClient.ts`
  - `src/lib/aiClient.ts`
  - `src/components/QAChat.tsx`
  - `src/pages/AIQA.tsx`
