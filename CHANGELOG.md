# 变更记录（CHANGELOG）

本文档按日期记录项目的重要变更。以下内容已与当前 Git 历史逐条对齐（截至 2026-03-16）。

## 2026-03-16（v2.4 - AI 对话稳定性调衡与 TXT 导出）

对应提交：`0d050196`

- AI 调衡：
  - 调整默认推理参数（`maxTokens`、`temperature`），在回答质量、稳定性和响应速度之间做平衡。
  - 客户端新增请求超时控制（`AbortController` + `timeoutMs`），避免长时间无响应。
  - 流式 SSE 解析改为缓冲区分帧解析，降低分包场景下的丢片段风险。
  - 当流式返回未产出正文时，自动回退到普通问答接口补全结果。
  - 增加“AI 正在思考中”占位展示，减少首字返回前的空白等待感。
- 新增功能：支持将当前 AI 对话导出为 `.txt` 文档。
  - 导出内容包含导出时间、AI 模式/模型、消息角色、时间戳、消息正文与 token 统计（如有）。
  - 聊天输入区新增“导出TXT”按钮。
- 测试与可测性增强：
  - 为 AI 客户端增加测试态 Mock 强制开关，避免单测误走真实 API。
  - `tests/aiClient.test.ts` 增加 `meta.mode === "mock"` 断言。
- 文档调整：
  - `README.md` 重写为更自然的项目手册表达，保留运行、配置、权限与验收信息。

## 2026-03-16（v2.3 - 账号权限与微纪录片互动增强）

对应提交：`2e61faae`

- 账号体系改为“账号与密码一致校验（不存储密码）”，并按账号规则自动推导角色。
- 学生/教师/管理员权限进一步收口，学生仅可修改本人姓名或头像。
- 反馈系统补全修改、状态变更、删除接口权限控制。
- 微纪录片新增评论区、点赞、实时同步（WebSocket）。
- 新增微纪录片相关后端接口与数据表（`microdoc_comments`、`microdoc_likes`）。
- 同步更新示例账号与文档说明。

## 2026-03-13（v2.2.1 - 部署修正与内容回填）

对应提交：`0f30ebf3`

- 修复部署入口相关问题，恢复应用可用启动路径。
- 回填并替换多组页面素材（首页、时间轴、微课、手册、微纪录片等）。
- 更新 `content/entries.json`、`content/lessons.json`、`content/handbook.json`、`content/microdoc.json`。
- 更新 `README.md`、`DEPLOY_MANUAL.md`、`Prompt编辑指南.md` 等文档。
- 调整 `server/db/index.js`、`server/scripts/seed-db.js` 等后端实现。

## 2026-03-12（v2.2 - 反馈系统持久化数据库）

对应提交：`354d048d`、`25a089b1`

- 后端反馈数据由内存 Mock 切换为 SQLite 文件持久化（`sql.js`）。
- 服务启动自动初始化数据库，默认文件 `server/data/feedback.db`。
- `auth`、`feedback`、`stats`、`users` 路由统一接入 `server/db/index.js`。
- 新增/更新数据库初始化与诊断脚本（含 `scripts/doctor-deps.mjs`）。
- 新增 `DEPLOY_MANUAL.md`，并完成部分历史文档整合。
- 同期引入 Unity AR 工程目录（后续于 2026-03-11 清理构建产物追踪）。

## 2026-03-11（v2.2.0-hotfix - 仓库体积治理）

对应提交：`7696b9d0`

- 停止追踪 Unity `Library` 构建产物。
- 更新 `.gitignore`，避免再次提交大体积自动生成文件。
- 降低仓库噪音与同步成本，改善协作稳定性。

## 2026-03-06（v2.1 - 部署前结构整理）

对应提交：`81dc8d83`

- 清理旧的静态入口与冗余资源（如旧 `script.js`、`styles.css` 等）。
- 强化反馈页与统计接口实现，补充 API 测试。
- 更新 `content/microdoc.json` 与部分页面组件，准备生产部署。

## 2026-03-01（v1.0 - 初始版本）

对应提交：`56740b24`、`7d8050d9`

- 创建项目初始代码：前端页面、后端服务、内容配置、测试与基础文档。
- 完成 AI 问答基础能力（Mock/真实接口切换、流式对话等）。
- 补充 `.gitignore` 与 `.specstory/.gitignore` 规则，规范仓库追踪范围。

## 历史说明（从旧文档迁移）

以下条目来源于早期阶段性说明文档，时间标签保留原文：

### 2025-10-15（v2.1 - 滚动体验修复版）

- 修复 AI 流式输出期间“滚轮被强制拉回底部”的问题。
- 自动滚动逻辑调整为“用户在底部时跟随，离开底部后暂停跟随”。

### 2025-10-15（v2.0 - 智能交互增强版）

- 快捷问题支持一键提问，并扩展问题数量。
- 优化 AI 回答风格与 Prompt 结构。
- 新增 Prompt 使用与修改说明文档。

### 2025-10（v1.0 - AI 问答配置完成）

- 完成 AI 问答模块真实接口接入（支持 Mock/真实模式切换）。
- 完成火山引擎客户端与统一 AI 客户端接入。
- 完成 AI 问答页面与聊天组件基础能力。
